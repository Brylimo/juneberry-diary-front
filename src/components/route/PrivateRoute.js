import React from 'react';
import { Route } from "react-router-dom";

/*const PrivateRoute = ({component: Component, ...remnant}) => {
    return (
        <Route {...remnant} element={props => (

        )}/>
    );
};*/